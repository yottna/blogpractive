server {
    listen 80;
    server_name hufsstore.shop;
    # 예시) server_name meotsa.com;

    # SSL 인증서 발급을 위한 경로
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}